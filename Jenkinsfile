pipeline {
    agent any
    stages {
        stage("Build") {
	    steps {
		echo "Build"
		}
	    }
	stage("Test") {
	    steps {
	        echo "Test"
		}
	    }
	stage("Docker build") {
	    steps {
		echo "Docker build"
		}
	    }
	stage("Deploy") {
	    steps {
		echo "Deploy"
		}
	    }
    }
}
	
